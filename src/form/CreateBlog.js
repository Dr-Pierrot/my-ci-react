import React from "react";
import TextField from "@mui/material/TextField";

const CreateBlog = ({name, label}) => {
    <TextField id={name} label={label} variant="outlined" />
};

export default CreateBlog;